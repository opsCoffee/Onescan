## **重要说明**

- **`.agent/` 目录严禁加入 `.gitignore`**，所有计划和进度文件必须纳入版本控制
- `.agent/` 目录用于存储分析报告、任务进度、执行计划等重要文档
- 这些文档必须提交到 Git 仓库，便于审阅和从历史 commit 中恢复复用

## 项目状态管理说明

本系统采用**双轨制状态管理**：

1. **`.agent/task_status.json`**：机器可读的结构化数据（AI 优先读取）
2. **`prompt.md`**：人类可读的任务列表（同步更新状态）

### 状态文件格式详解

`.agent/task_status.json` 完整结构：

```json
{
  "version": "1.0",
  "last_update": "2025-12-03T08:54:00+08:00",
  
  // 当前执行位置
  "current_phase": "1.2",
  "current_task": "SECURITY-001",
  
  // 任务分类
  "completed_phases": ["1.1"],
  "completed_tasks": ["CLIPPY-1", "CLIPPY-2", ...],
  "in_progress_tasks": ["SECURITY-001"],
  "skipped_tasks": [],
  "failed_tasks": [],
  
  // 任务详情（每个任务的元数据）
  "task_details": {
    "CLIPPY-1": {
      "status": "completed",
      "completed_at": "2025-12-02T17:15:00+08:00",
      "commit": "a462fe5",
      "description": "文档注释格式问题"
    },
    "SECURITY-001": {
      "status": "in_progress",
      "started_at": "2025-12-03T08:54:00+08:00",
      "description": "密码明文存储"
    }
  },
  
  // 统计信息
  "total_tasks": 35,
  "completed_count": 7,
  "in_progress_count": 1,
  "pending_count": 27,
  "progress_percentage": 20,
  
  // 阶段信息
  "phases": {
    "1.1": {
      "name": "Clippy 错误修复",
      "status": "completed",
      "total_tasks": 7,
      "completed_tasks": 7
    },
    "1.2": {
      "name": "高风险问题修复",
      "status": "in_progress",
      "total_tasks": 8,
      "completed_tasks": 0
    }
  }
}
```

## 执行前置要求

**必须严格遵守以下执行顺序**：

1. **读取任务状态**：
   - 检查 `.agent/task_status.json` 是否存在
   - 如果不存在，创建初始状态文件
   - 如果存在，读取当前进度并跳过已完成任务

2. **深度思考模式**：
   - 在执行任何任务前，必须先调用 `sequential-thinking` MCP 服务器进行深度思考
   - 思考内容：澄清任务本质、分析依赖关系、规划执行策略
   - 输出思考结果到 `.agent/thinking.md`

3. **任务拆分决策**：
   - 分析任务是否可以拆分成互不干扰的子任务
   - 如果可以拆分：并行调用子代理（subsAgent）执行
   - 如果不可拆分：串行执行
   - 记录拆分决策到 `.agent/task_plan.md`

4. **进度追踪**：
   - 检查 `.agent/task.md` 文件，读取当前阶段和进度
   - 如果文件不存在，创建初始进度文件
   - **每完成一个任务，立即更新以下文件**：
     - `.agent/task_status.json`（机器可读）
     - `.agent/task.md`（人类可读）
     - `prompt.md`（更新任务复选框状态）

## 执行规则

**请严格遵循以下 Skill 中定义的规范**：

- 📋 **执行流程**：参考 `.claude/skills/agent-workflow/SKILL.md`
- 🧪 **测试验证**：参考 `.claude/skills/rust-testing/SKILL.md`
- 📝 **报告格式**：参考 `.claude/skills/report-templates/SKILL.md`
- 🔖 **Git 规范**：参考 `.claude/skills/git-commit/SKILL.md`
- 📊 **进度追踪**：参考 `.claude/skills/progress-tracking/SKILL.md`
- ✅ **审查标准**：参考 `.claude/skills/rust-code-review/SKILL.md`

## 执行约束

- **时间限制**：6 小时
- **超时处理**：提交已完成工作并退出
- **任务范围**：不要添加 TODO 外的任务
- **高风险任务**：不要执行标记为 " 不执行 " 的高风险任务
- **状态同步**：每完成一个任务必须同步更新所有状态文件